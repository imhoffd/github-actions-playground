name: Test Workflow

on:
  push:
    branches:
      - '**'

jobs:
  test:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - run: git config --list
      - run: echo hello > hello.txt
      - run: git add hello.txt
      - run: git commit -m "hello"
      - run: git push
