name: Test Workflow

on:
  push:
    branches:
      - '**'

jobs:
  test:
    name: Test Job
    runs-on: windows-latest
    steps:
      - run: echo $Env:Path
      - run: dir
        working-directory: 'C:\Program Files (x86)\Microsoft Visual Studio\2017\Enterprise'
