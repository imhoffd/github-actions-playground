name: Test Workflow

on:
  push:
    branches:
      - '**'

jobs:
  test:
    name: Test Job
    runs-on: windows-latest
    steps:
      - run: echo $Env:Path
      - run: cd 'C:\Program Files (x86)\Microsoft SDKs\10\bin' ; tree
